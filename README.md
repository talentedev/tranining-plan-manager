# tranining-plan-manager

## Setup
Please install node modules on `frontend` and `backend` folder.

```
npm install
```

## Run project
Please run frontend and backend
```
npm start
```

If everything is OK, visit `http://localhost:8080`
